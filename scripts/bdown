# !/usr/bin/env bash
bash -c 'b=$(xrandr --verbose | grep -i brightness | head -n 1 | cut -d" " -f2) && b2=$(echo -e "import math\nf = lambda x: (1 - 1/(1+math.exp(4*x-2)))\nprint(\"%.2f\" % f(-0.1+$b))" | python) && xrandr --output eDP-1 --brightness $b2'

